

> [!Warning]
> **Modification Summary:** <br>
> This modified version only restores the functionality of `RENDEZVOUS_SERVER` and `RS_PUB_KEY` during Action compilation, with no other changes made to the code. You can fork this project and define `RENDEZVOUS_SERVER` and `RS_PUB_KEY` yourself to generate versions with the built-in server and Pub Key. The authors are not responsible for any misuse of the application.
